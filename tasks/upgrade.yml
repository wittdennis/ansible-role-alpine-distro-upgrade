# code: language=ansible
---
- name: Update repositories file
  become: true
  ansible.builtin.replace:
    regexp: v\d+\.\d+
    path: /etc/apk/repositories
    replace: "v{{ alpine_distro_version_desired }}"

- name: Update apk-tools
  become: true
  community.general.apk:
    update_cache: true
    name:
      - apk-tools
    state: latest

- name: Update installed packages
  become: true
  register: alpine_package_upgrade
  community.general.apk:
    update_cache: true
    upgrade: true
    available: true

- name: Sync and reboot # noqa: no-changed-when no-handler
  become: true
  when: alpine_package_upgrade.changed
  # raw (not the reboot module) so this survives a distro upgrade replacing Python.
  # Backgrounded so the command returns before the connection drops (raw has no async).
  ansible.builtin.raw: sync && (sleep 2 && reboot) &

- name: Wait for system to come back online # noqa: no-handler
  when: alpine_package_upgrade.changed
  ansible.builtin.wait_for_connection:
    delay: 15
    timeout: 300
